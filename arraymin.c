#include<stdio.h>
int main()
{
int A[25],size,min,c;
printf("\n enterthe size of thearray:");
scanf("%d",&size);
printf("\n enter the integers for the array");
for(c=o;c<size;c++)
scanf("%d",&arr[c]);
min=array[0];
for(c=1;c<size;c++)
{
if(array[c]<min)
{
min=array[c];
}
}
printf("\n minimum element present in the array is %d",max);
return 0;
}
