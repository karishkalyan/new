#include<stdio.h>
void main()
{
int A[25],size,max,c;
printf("\n enterthe size of thearray:");
scanf("%d",&size);
printf("\n enter the integers for the array");
for(c=o;c<size;c++)
scanf("%d",&arr[c]);
max=array[0];
for(c=1;c<size;c++)
{
if(array[c]>max)
{
max=array[c];
}
}
printf("\n maxelement present in the array is %d",max);
getch();
}
